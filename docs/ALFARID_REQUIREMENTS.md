# ALFARID v1.0 — Полные технические требования

## 📋 Оглавление

1. [Описание проекта](#описание-проекта)
2. [Системные требования](#системные-требования)
3. [Архитектура](#архитектура)
4. [Сравнительный анализ: Линко vs Alfarid](#сравнительный-анализ)
5. [Модули и функции](#модули-и-функции)
6. [UI/UX дизайн](#uiux-дизайн)
7. [Сетевой протокол](#сетевой-протокол)
8. [База данных](#база-данных)
9. [Сборка и установщик](#сборка-и-установщик)
10. [План разработки](#план-разработки)

---

## 🎯 Описание проекта

**ALFARID** — улучшенная система управления компьютерным лингафонным кабинетом с открытым исходным кодом. Является современной альтернативой коммерческому ПО Линко V8.3/V8.5.

### Цели проекта:
- Полная замена Линко V8.3/V8.5
- Бесплатное и открытое ПО (GPL v3)
- Современный UI/UX
- Кроссплатформенность (Python + PyQt5)
- Простота установки и использования

### Целевая аудитория:
- Образовательные учреждения
- Языковые школы
- Корпоративные учебные центры
- Репетиторы

---

## 💻 Системные требования

### Преподаватель (Сервер):
| Параметр | Минимум | Рекомендуется |
|----------|---------|---------------|
| ОС | Windows 7 SP1 | Windows 10/11 |
| CPU | Intel Core 2 Duo 2.0 ГГц | Intel Core i3+ |
| RAM | 2 ГБ | 4 ГБ |
| Диск | 500 МБ | 2 ГБ |
| Сеть | 100 Mbps Ethernet | 1 Gbps Ethernet |
| Разрешение | 1024x768 | 1920x1080 |

### Студент (Клиент):
| Параметр | Минимум | Рекомендуется |
|----------|---------|---------------|
| ОС | Windows 7 SP1 | Windows 10/11 |
| CPU | Pentium 4 1.5 ГГц | Intel Core i3 |
| RAM | 512 МБ | 2 ГБ |
| Диск | 200 МБ | 500 МБ |
| Сеть | 100 Mbps Ethernet | WiFi 802.11n |

### Поддерживаемые ОС:
- Windows 7/8/8.1/10/11 (32 и 64 бит)
- *Возможно в будущем: Linux, macOS*

---

## 🏗️ Архитектура

### Компоненты системы:

```
┌─────────────────────────────────────────────────────────────────┐
│                        ALFARID SYSTEM                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────┐         ┌─────────────────────────────┐   │
│  │  TEACHER APP    │◄───────►│     NETWORK LAYER           │   │
│  │  (teacher_app)  │         │  TCP/UDP + Multicast        │   │
│  └─────────────────┘         └─────────────────────────────┘   │
│         │                              │                        │
│         ▼                              ▼                        │
│  ┌─────────────────┐         ┌─────────────────────────────┐   │
│  │  - Main Window  │         │     STUDENT APPs (1-50)     │   │
│  │  - Toolbar      │         │     (student_app)           │   │
│  │  - Plan класса  │         └─────────────────────────────┘   │
│  │  - Контроль     │                   │                        │
│  └─────────────────┘                   ▼                        │
│         │                    ┌─────────────────────────────┐   │
│         ▼                    │  - Main Window              │   │
│  ┌─────────────────┐         │  - Плавающая панель         │   │
│  │  MODULES        │         │  - Системный трей           │   │
│  │  - Streaming    │         │  - Область трансляции       │   │
│  │  - Audio Lab    │         └─────────────────────────────┘   │
│  │  - Exams        │                                            │
│  │  - Files        │         ┌─────────────────────────────┐   │
│  │  - Control      │         │     DATABASE (SQLite)       │   │
│  │  - Whiteboard   │◄───────►│  - students, groups         │   │
│  └─────────────────┘         │  - exams, results           │   │
│                              │  - courses, attendance      │   │
│                              └─────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### Технологический стек:

| Компонент | Технология |
|-----------|------------|
| Язык | Python 3.9+ |
| GUI Framework | PyQt5 5.15+ |
| Сеть | socket (TCP/UDP), Multicast |
| Видео | OpenCV, mss |
| Аудио | sounddevice, soundfile |
| БД | SQLite3 |
| Сборка | PyInstaller |
| Установщик | Inno Setup / NSIS |

---

## 📊 Сравнительный анализ

### Линко V8.3/V8.5 vs Alfarid v1.0

| Функция | Линко V8.3 | Alfarid (есть) | Alfarid (надо) |
|---------|:----------:|:--------------:|:--------------:|
| **ИНТЕРАКТИВНОЕ ОБУЧЕНИЕ** ||||
| Трансляция экрана преподавателя | ✅ | ✅ | — |
| Трансляция голоса при трансляции | ✅ | ❌ | 🔴 Сделать |
| Электронное перо для комментариев | ✅ | ❌ | 🔴 Сделать |
| Ученик управляет ПК преподавателя | ✅ | ❌ | 🟡 v1.1 |
| Запись голоса+видео экрана | ✅ | ❌ | 🔴 Сделать |
| Окно/полный экран | ✅ | ✅ | — |
| Настройки качества изображения | ✅ | ✅ | — |
| **ВИДЕО ПО СЕТИ** ||||
| Трансляция видеофайлов | ✅ | ✅ (код) | 🟡 UI |
| Все форматы видео | ✅ | ✅ | — |
| Плейлист (добавить/удалить) | ✅ | ❌ | 🔴 Сделать |
| Режимы повтора | ✅ | ❌ | 🟡 v1.1 |
| Управление громкостью | ✅ | ❌ | 🔴 Сделать |
| **ВЕБ-КАМЕРА** ||||
| Трансляция с веб-камеры | ✅ | ❌ | 🔴 Сделать |
| Capture box/dongle | ✅ | ❌ | 🟡 v1.2 |
| **ДЕМОНСТРАЦИЯ СТУДЕНТА** ||||
| Трансляция экрана студента | ✅ | ❌ (заглушка) | 🔴 Сделать |
| Наблюдение за студентом | ✅ | ❌ (заглушка) | 🔴 Сделать |
| Удаленное управление ПК студента | ✅ | ❌ | 🟡 v1.1 |
| Запись экрана студента | ✅ | ❌ | 🔴 Сделать |
| Прямое общение с учеником | ✅ | ✅ (чат) | — |
| **ЧАТ** ||||
| Текстовые сообщения | ✅ | ✅ | — |
| Чат по группам | ✅ | ❌ | 🔴 Сделать |
| Чат по темам | ✅ | ❌ | 🟡 v1.1 |
| Графика/изображения в чате | ✅ | ❌ | 🟡 v1.1 |
| Голосовой чат | ✅ | ❌ | 🔴 Сделать |
| История чата | ✅ | ❌ | 🔴 Сделать |
| **ВИКТОРИНА** ||||
| Быстрый ответ (кто первый) | ✅ | ❌ | 🔴 Сделать |
| Автооценка | ✅ | ✅ | — |
| Демонстрация экрана лидера | ✅ | ❌ | 🟡 v1.1 |
| Система награждений | ✅ | ❌ | 🟡 v1.2 |
| **ГРУППЫ** ||||
| Создание групп | ✅ | ✅ | — |
| Случайные группы | ✅ | ✅ | — |
| Лидер группы | ✅ | ✅ | — |
| Наблюдение за группами | ✅ | ✅ (код) | 🟡 UI |
| **ИНТЕРАКТИВНАЯ ДОСКА** ||||
| Пустая доска для рисования | ✅ | ❌ | 🔴 Сделать |
| Импорт изображения | ✅ | ❌ | 🔴 Сделать |
| Инструменты рисования | ✅ | ❌ | 🔴 Сделать |
| Совместное рисование | ✅ | ❌ | 🔴 Сделать |
| История доски | ✅ | ❌ | 🟡 v1.1 |
| **ЦИФРОВОЙ МАГНИТОФОН** ||||
| 2-дорожечный магнитофон | ✅ | ✅ (код) | 🟡 UI |
| Аудиограф | ✅ | ❌ | 🔴 Сделать |
| Метки для прослушивания | ✅ | ✅ (код) | — |
| Субтитры | ✅ | ✅ (код) | — |
| Редактор курса | ✅ | ❌ | 🟡 v1.1 |
| Воспроизведение-Запись-Воспр. | ✅ | ✅ | — |
| Внешние источники (CD, радио) | ✅ | ❌ | 🟡 v1.2 |
| **ЭКЗАМЕН/ТЕСТ** ||||
| Создание бланка | ✅ | ✅ | — |
| 5 типов вопросов | ✅ | ✅ | — |
| Автопроверка | ✅ | ✅ | — |
| Ручная проверка | ✅ | ✅ | — |
| Экспорт HTML | ✅ | ❌ | 🔴 Сделать |
| Статистика (графики) | ✅ | ❌ | 🔴 Сделать |
| Импорт из документа | ✅ | ❌ | 🟡 v1.1 |
| **ОПРОС** ||||
| Быстрый опрос | ✅ | ✅ | — |
| 2 типа вопросов | ✅ | ✅ | — |
| Время на обдумывание | ✅ | ❌ | 🔴 Сделать |
| Результаты (графики) | ✅ | ❌ | 🔴 Сделать |
| **НАБЛЮДЕНИЕ** ||||
| Наблюдение за 1+ студентами | ✅ | ❌ | 🔴 Сделать |
| Автонаблюдение (интервал) | ✅ | ❌ | 🟡 v1.1 |
| Снимок экрана | ✅ | ❌ | 🔴 Сделать |
| Видеозапись экрана | ✅ | ❌ | 🔴 Сделать |
| **НАСТРОЙКИ УЧЕНИКА** ||||
| Блокировка интернета | ✅ | ✅ (код) | 🟡 UI |
| Белый/черный список сайтов | ✅ | ✅ (код) | 🟡 UI |
| Блокировка USB/CD | ✅ | ❌ | 🟡 v1.1 |
| Блокировка принтера | ✅ | ❌ | 🟡 v1.2 |
| **МОДЕЛЬ КЛАССА** ||||
| План класса (значки) | ✅ | ✅ | — |
| Автопривязка студентов | ✅ | ✅ | — |
| Расстановка значков | ✅ | ❌ | 🔴 Сделать |
| Размер значков | ✅ | ❌ | 🔴 Сделать |
| Сортировка значков | ✅ | ❌ | 🔴 Сделать |
| Уровень заряда батареи | ✅ | ❌ | 🟡 v1.1 |
| **УПРАВЛЕНИЕ КЛАССОМ** ||||
| Сохранение модели класса | ✅ | ❌ | 🔴 Сделать |
| Импорт/экспорт модели | ✅ | ❌ | 🔴 Сделать |
| **ПОДКЛЮЧЕНИЕ** ||||
| Импорт списка студентов | ✅ | ❌ | 🔴 Сделать |
| Время подключения (опоздания) | ✅ | ❌ | 🔴 Сделать |
| Вход по паролю | ✅ | ❌ | 🔴 Сделать |
| Проверка посещаемости | ✅ | ✅ (БД) | 🟡 UI |
| **ПЕРЕДАЧА ФАЙЛОВ** ||||
| Отправка файлов/папок | ✅ | ✅ (код) | 🔴 Сеть |
| Прогресс передачи | ✅ | ✅ | — |
| Ограничение размера | ✅ | ✅ | — |
| **УДАЛЕННЫЕ КОМАНДЫ** ||||
| Открыть приложение | ✅ | ❌ | 🔴 Сделать |
| Открыть веб-сайт | ✅ | ❌ | 🔴 Сделать |
| Выключение ПК | ✅ | ✅ (код) | — |
| Перезагрузка ПК | ✅ | ✅ (код) | — |
| Закрыть приложение | ✅ | ❌ | 🔴 Сделать |
| **БЛОКИРОВКА** ||||
| Блокировка экрана | ✅ | ✅ | — |
| Сообщение на экране | ✅ | ✅ | — |
| Блокировка клавиатуры/мыши | ✅ | ❌ | 🔴 Сделать |
| **СООБЩЕНИЯ** ||||
| Поднятие руки | ✅ | ✅ | — |
| Отправка сообщений | ✅ | ✅ | — |
| Готовые сообщения | ✅ | ❌ | 🔴 Сделать |
| **УСТАНОВЩИК** ||||
| EXE установщик для Windows | ✅ | ❌ | 🔴 Сделать |
| Автозапуск | ✅ | ❌ | 🔴 Сделать |

### Легенда:
- ✅ Реализовано
- ❌ Не реализовано
- 🔴 Приоритет v1.0 (критично)
- 🟡 Запланировано на v1.1/v1.2

---

## 📦 Модули и функции

### МОДУЛЬ 1: Трансляция экрана (Screen Broadcasting)

**Файлы:** `src/streaming/screen_capture.py`

**Что есть:**
- ✅ Захват экрана через mss
- ✅ 4 уровня качества (LOW/MEDIUM/HIGH/ULTRA)
- ✅ JPEG сжатие
- ✅ Контроль FPS
- ✅ Прием и отображение на стороне студента

**Что нужно сделать:**
- 🔴 **Трансляция голоса** одновременно с экраном
  - Захват с микрофона (sounddevice)
  - Сжатие аудио (Opus/AAC)
  - Синхронизация аудио/видео
- 🔴 **Электронное перо**
  - Рисование поверх экрана
  - Инструменты: перо, маркер, стрелка, прямоугольник, текст
  - Отправка аннотаций студентам
- 🔴 **Запись урока в файл**
  - Запись экрана + аудио в MP4/AVI
  - Использование OpenCV VideoWriter
  - Настройка качества записи
- 🔴 **Исправить TCP буферизацию**
  - Сборка фрагментированных пакетов
  - Надежная передача больших кадров

**Код для буферизации:**
```python
class TCPPacketAssembler:
    """Сборщик TCP пакетов"""
    def __init__(self):
        self.buffer = b''
    
    def feed(self, data: bytes) -> list:
        """Добавить данные и вернуть готовые пакеты"""
        self.buffer += data
        packets = []
        
        while len(self.buffer) >= Protocol.HEADER_SIZE:
            # Читаем заголовок
            header = self.buffer[:Protocol.HEADER_SIZE]
            magic, version, length, compressed = struct.unpack(
                Protocol.HEADER_FORMAT, header
            )
            
            if magic != Protocol.MAGIC:
                # Синхронизация потеряна, ищем magic
                idx = self.buffer.find(Protocol.MAGIC)
                if idx > 0:
                    self.buffer = self.buffer[idx:]
                    continue
                else:
                    self.buffer = b''
                    break
            
            total_size = Protocol.HEADER_SIZE + length
            if len(self.buffer) < total_size:
                break  # Ждем больше данных
            
            packet = self.buffer[:total_size]
            self.buffer = self.buffer[total_size:]
            packets.append(packet)
        
        return packets
```

---

### МОДУЛЬ 2: Веб-камера (Webcam)

**Файлы:** `src/streaming/webcam.py` (создать)

**Что нужно сделать:**
```python
import cv2

class WebcamCapture:
    """Захват с веб-камеры"""
    
    def __init__(self, device_id: int = 0, resolution: tuple = (640, 480)):
        self.device_id = device_id
        self.resolution = resolution
        self.cap = None
        self.capturing = False
        self.on_frame = None  # Callback
    
    def start(self) -> bool:
        """Начать захват"""
        self.cap = cv2.VideoCapture(self.device_id)
        self.cap.set(cv2.CAP_PROP_FRAME_WIDTH, self.resolution[0])
        self.cap.set(cv2.CAP_PROP_FRAME_HEIGHT, self.resolution[1])
        
        if not self.cap.isOpened():
            return False
        
        self.capturing = True
        # Запустить поток захвата...
        return True
    
    def get_available_cameras(self) -> list:
        """Получить список доступных камер"""
        cameras = []
        for i in range(10):
            cap = cv2.VideoCapture(i)
            if cap.isOpened():
                cameras.append(i)
                cap.release()
        return cameras
```

---

### МОДУЛЬ 3: Интерактивная доска (Whiteboard)

**Файлы:** `src/whiteboard/whiteboard.py` (создать)

**Функции:**
- Холст для рисования
- Инструменты: карандаш, кисть, ластик, фигуры, текст
- Выбор цвета и толщины
- Отмена/повтор (undo/redo)
- Импорт изображений как фон
- Совместное рисование (синхронизация)
- Экспорт в PNG/PDF

**Схема реализации:**
```python
from PyQt5.QtWidgets import QWidget
from PyQt5.QtGui import QPainter, QPen, QImage
from PyQt5.QtCore import Qt, QPoint

class WhiteboardWidget(QWidget):
    """Виджет интерактивной доски"""
    
    def __init__(self):
        super().__init__()
        self.image = QImage(1920, 1080, QImage.Format_RGB32)
        self.image.fill(Qt.white)
        
        self.drawing = False
        self.last_point = QPoint()
        
        # Инструменты
        self.tool = "pen"  # pen, brush, eraser, line, rect, ellipse, text
        self.pen_color = Qt.black
        self.pen_width = 3
        
        # История для undo/redo
        self.history = []
        self.history_index = -1
    
    def paintEvent(self, event):
        painter = QPainter(self)
        painter.drawImage(self.rect(), self.image)
    
    def mousePressEvent(self, event):
        if event.button() == Qt.LeftButton:
            self.drawing = True
            self.last_point = event.pos()
            self._save_history()
    
    def mouseMoveEvent(self, event):
        if self.drawing:
            painter = QPainter(self.image)
            pen = QPen(self.pen_color, self.pen_width, Qt.SolidLine)
            painter.setPen(pen)
            painter.drawLine(self.last_point, event.pos())
            self.last_point = event.pos()
            self.update()
            
            # Отправить штрих другим
            self._broadcast_stroke(...)
    
    def _save_history(self):
        """Сохранить состояние для undo"""
        self.history = self.history[:self.history_index + 1]
        self.history.append(self.image.copy())
        self.history_index = len(self.history) - 1
    
    def undo(self):
        if self.history_index > 0:
            self.history_index -= 1
            self.image = self.history[self.history_index].copy()
            self.update()
```

---

### МОДУЛЬ 4: Голосовой чат (Voice Chat)

**Файлы:** `src/audio/voice_chat.py` (создать)

**Функции:**
- Push-to-talk (нажми чтобы говорить)
- Полнодуплексная связь
- Индикатор уровня звука
- Подавление шума

**Реализация:**
```python
import sounddevice as sd
import numpy as np

class VoiceChat:
    """Голосовой чат"""
    
    SAMPLE_RATE = 16000
    CHANNELS = 1
    CHUNK_SIZE = 1024
    
    def __init__(self):
        self.recording = False
        self.playing = False
        self.on_audio_data = None  # Callback для отправки
    
    def start_recording(self):
        """Начать запись и отправку"""
        def callback(indata, frames, time, status):
            if self.recording:
                # Сжать и отправить
                compressed = self._compress(indata)
                if self.on_audio_data:
                    self.on_audio_data(compressed)
        
        self.stream = sd.InputStream(
            samplerate=self.SAMPLE_RATE,
            channels=self.CHANNELS,
            callback=callback
        )
        self.stream.start()
        self.recording = True
    
    def play_audio(self, data: bytes):
        """Воспроизвести полученные аудиоданные"""
        audio = self._decompress(data)
        sd.play(audio, self.SAMPLE_RATE)
    
    def _compress(self, audio: np.ndarray) -> bytes:
        """Сжать аудио (простое сжатие)"""
        # Можно использовать zlib или Opus
        import zlib
        return zlib.compress(audio.tobytes())
    
    def _decompress(self, data: bytes) -> np.ndarray:
        """Распаковать аудио"""
        import zlib
        raw = zlib.decompress(data)
        return np.frombuffer(raw, dtype=np.float32)
```

---

### МОДУЛЬ 5: Наблюдение за студентами (Monitoring)

**Файлы:** `src/control/monitoring.py` (создать)

**Функции:**
- Просмотр миниатюр экранов студентов
- Увеличение выбранного студента
- Циклическое переключение
- Скриншот экрана студента

**Реализация:**
```python
class StudentMonitor:
    """Монитор экранов студентов"""
    
    def __init__(self, server):
        self.server = server
        self.thumbnails = {}  # student_id -> QPixmap
        self.update_interval = 5  # секунд
        self.selected_student = None
    
    def request_screenshot(self, student_id: str):
        """Запросить скриншот у студента"""
        self.server.send_to_student(
            student_id,
            MessageType.SCREENSHOT_REQUEST,
            {"quality": 50}
        )
    
    def request_all_thumbnails(self):
        """Запросить миниатюры у всех студентов"""
        for student in self.server.get_students():
            self.request_screenshot(student.id)
    
    def start_auto_refresh(self, interval: int = 5):
        """Автообновление миниатюр"""
        self.timer = QTimer()
        self.timer.timeout.connect(self.request_all_thumbnails)
        self.timer.start(interval * 1000)
```

---

### МОДУЛЬ 6: Викторина (Quiz Competition)

**Файлы:** `src/exams/quiz.py` (создать)

**Функции:**
- Режим "Кто первый" (нажми кнопку)
- Таймер ответа
- Система баллов/награждений
- Лидерборд

**Реализация:**
```python
class QuizCompetition:
    """Викторина с соревнованием"""
    
    def __init__(self):
        self.question = None
        self.answers = {}  # student_id -> (answer, timestamp)
        self.first_responder = None
        self.start_time = None
        self.time_limit = 30  # секунд
    
    def start_question(self, question: str, options: list):
        """Начать вопрос"""
        self.question = question
        self.answers.clear()
        self.first_responder = None
        self.start_time = time.time()
        
        # Отправить всем студентам
        payload = {
            "question": question,
            "options": options,
            "time_limit": self.time_limit
        }
        # broadcast...
    
    def receive_answer(self, student_id: str, answer: str):
        """Получить ответ от студента"""
        if student_id in self.answers:
            return  # Уже ответил
        
        timestamp = time.time() - self.start_time
        self.answers[student_id] = (answer, timestamp)
        
        if self.first_responder is None:
            self.first_responder = student_id
            # Уведомить что он первый
    
    def get_leaderboard(self) -> list:
        """Получить таблицу лидеров"""
        sorted_answers = sorted(
            self.answers.items(),
            key=lambda x: x[1][1]  # По времени
        )
        return [(sid, ans, ts) for sid, (ans, ts) in sorted_answers]
```

---

### МОДУЛЬ 7: Удаленные команды (Remote Commands)

**Файлы:** `src/control/remote_commands.py` (создать)

**Команды:**
- Открыть приложение
- Открыть веб-сайт
- Закрыть приложение
- Выключить/перезагрузить ПК
- Заблокировать клавиатуру/мышь

**Реализация на стороне студента:**
```python
import subprocess
import os
import ctypes

class RemoteCommandExecutor:
    """Исполнитель удаленных команд"""
    
    def execute(self, command: str, params: dict):
        if command == "open_app":
            self._open_application(params.get("path"))
        elif command == "open_url":
            self._open_url(params.get("url"))
        elif command == "close_app":
            self._close_application(params.get("process_name"))
        elif command == "shutdown":
            self._shutdown()
        elif command == "restart":
            self._restart()
        elif command == "lock_input":
            self._lock_input(params.get("lock", True))
    
    def _open_application(self, path: str):
        os.startfile(path)
    
    def _open_url(self, url: str):
        import webbrowser
        webbrowser.open(url)
    
    def _close_application(self, process_name: str):
        subprocess.run(["taskkill", "/f", "/im", process_name])
    
    def _shutdown(self):
        subprocess.run(["shutdown", "/s", "/t", "30"])
    
    def _restart(self):
        subprocess.run(["shutdown", "/r", "/t", "30"])
    
    def _lock_input(self, lock: bool):
        """Заблокировать клавиатуру и мышь"""
        if lock:
            ctypes.windll.user32.BlockInput(True)
        else:
            ctypes.windll.user32.BlockInput(False)
```

---

### МОДУЛЬ 8: Плавающая панель студента (Floating Toolbar)

**Файлы:** `src/student/floating_toolbar.py` (создать)

**Согласно руководству Линко, панель должна содержать:**
- Скрыть/показать панель
- Поднять руку
- Отправить сообщение
- Отправить файл
- Просмотреть полученные файлы
- Справка

**Реализация:**
```python
from PyQt5.QtWidgets import QWidget, QHBoxLayout, QPushButton
from PyQt5.QtCore import Qt

class FloatingToolbar(QWidget):
    """Плавающая панель студента"""
    
    def __init__(self, parent=None):
        super().__init__(parent)
        self.setWindowFlags(
            Qt.WindowStaysOnTopHint | 
            Qt.FramelessWindowHint |
            Qt.Tool
        )
        self.setAttribute(Qt.WA_TranslucentBackground)
        
        self._init_ui()
        self._position_at_top()
    
    def _init_ui(self):
        layout = QHBoxLayout()
        layout.setContentsMargins(5, 5, 5, 5)
        
        self.toggle_btn = QPushButton("≡")
        self.raise_hand_btn = QPushButton("🖐️")
        self.message_btn = QPushButton("💬")
        self.send_file_btn = QPushButton("📤")
        self.received_files_btn = QPushButton("📥")
        self.help_btn = QPushButton("❓")
        
        for btn in [self.toggle_btn, self.raise_hand_btn, 
                    self.message_btn, self.send_file_btn,
                    self.received_files_btn, self.help_btn]:
            btn.setFixedSize(40, 40)
            layout.addWidget(btn)
        
        self.setLayout(layout)
    
    def _position_at_top(self):
        """Позиционировать вверху экрана"""
        from PyQt5.QtWidgets import QDesktopWidget
        desktop = QDesktopWidget()
        screen = desktop.screenGeometry()
        x = (screen.width() - self.width()) // 2
        self.move(x, 0)
```

---

## 🎨 UI/UX дизайн

### Цветовая схема

```
Основные цвета:
- Primary:    #2563EB (синий)
- Secondary:  #10B981 (зеленый)
- Accent:     #F59E0B (оранжевый)
- Danger:     #EF4444 (красный)
- Background: #F3F4F6 (светло-серый)
- Surface:    #FFFFFF (белый)
- Text:       #1F2937 (темно-серый)

Темная тема:
- Background: #111827
- Surface:    #1F2937
- Text:       #F9FAFB
```

### Иконки

Использовать: 
- Emoji для быстрой разработки (🖐️📺📝💬📁)
- Material Icons или Feather Icons для финальной версии
- Собственные иконки SVG для брендинга

### Шрифты

```
- Основной: Segoe UI (Windows), Inter (веб)
- Моноширинный: Consolas, JetBrains Mono
- Размеры: 10pt (обычный), 12pt (заголовки), 14pt (большие заголовки)
```

### Макеты окон

**Окно преподавателя:**
```
┌─────────────────────────────────────────────────────────────┐
│ [Меню] [Трансляция] [Видео] [Магнитофон] [Экзамен] [Группы] │
│         [Опрос] [Файлы] [Наблюдение] [Отчеты] [Настройки]   │
├─────────────────────────────────────────────────────────────┤
│                        ПЛАН КЛАССА                          │
│  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐      │
│  │ 📺  │  │ 📺  │  │ 📺  │  │ 📺  │  │ 📺  │  │ 📺  │      │
│  │Иван │  │Петр │  │Анна │  │Олег │  │Мария│  │Денис│      │
│  │Онлайн│  │Онлайн│  │Экзам│  │Онлайн│  │Офф │  │Онлайн│      │
│  └─────┘  └─────┘  └─────┘  └─────┘  └─────┘  └─────┘      │
│                                                              │
│  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐                        │
│  │ 📺  │  │ 📺  │  │ 📺  │  │ 📺  │                        │
│  │Елена│  │Сергей│  │Юлия │  │Макс │                        │
│  │Онлайн│  │🖐️   │  │Онлайн│  │Онлайн│                        │
│  └─────┘  └─────┘  └─────┘  └─────┘                        │
├─────────────────────────────────────────────────────────────┤
│ Подключено: 8/10 | Время: 14:35:22 | IP: 192.168.1.100:9999  │
└─────────────────────────────────────────────────────────────┘
```

**Окно студента:**
```
┌─────────────────────────────────────────┐
│         ALFARID - Студент: Иван         │
├─────────────────────────────────────────┤
│           🟢 Подключен                  │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │                                   │  │
│  │     ОБЛАСТЬ ТРАНСЛЯЦИИ ЭКРАНА    │  │
│  │                                   │  │
│  │                                   │  │
│  └───────────────────────────────────┘  │
├─────────────────────────────────────────┤
│  Преподаватель: Иванов И.В.             │
│  IP: 192.168.1.100:9999                 │
├─────────────────────────────────────────┤
│  [🖐️ Поднять руку] [💬 Сообщение]       │
├─────────────────────────────────────────┤
│  Сообщения:                             │
│  [14:30] Преподаватель: Добрый день!    │
│  [14:31] Вы: Здравствуйте!              │
├─────────────────────────────────────────┤
│  [Ввод сообщения...           ] [Отпр.] │
└─────────────────────────────────────────┘
```

---

## 🌐 Сетевой протокол

### Порты:
| Протокол | Порт | Назначение |
|----------|------|------------|
| TCP | 9999 | Основные соединения |
| UDP | 10000 | Multicast обнаружение |
| UDP | 10001 | Аудио стриминг |
| TCP | 10002 | Передача файлов |

### Формат пакета:
```
┌─────────────────────────────────────────────────────┐
│ MAGIC (4B) │ VERSION (2B) │ LENGTH (4B) │ FLAGS (1B) │
├─────────────────────────────────────────────────────┤
│                    PAYLOAD (N bytes)                │
└─────────────────────────────────────────────────────┘

MAGIC: "AFRD" (0x41465244)
VERSION: 1
FLAGS: 
  bit 0: compressed (zlib)
  bit 1: encrypted
  bit 2: fragmented
```

### Типы сообщений (расширенные):

```python
class MessageType:
    # Соединение
    TEACHER_BROADCAST = "TEACHER_BROADCAST"
    STUDENT_CONNECT = "STUDENT_CONNECT"
    CONNECTION_ACCEPTED = "CONNECTION_ACCEPTED"
    CONNECTION_REJECTED = "CONNECTION_REJECTED"
    PING = "PING"
    PONG = "PONG"
    
    # Трансляция экрана
    SCREEN_STREAM_START = "SCREEN_STREAM_START"
    SCREEN_STREAM_STOP = "SCREEN_STREAM_STOP"
    SCREEN_FRAME = "SCREEN_FRAME"
    
    # Аудио
    AUDIO_STREAM_START = "AUDIO_STREAM_START"
    AUDIO_STREAM_STOP = "AUDIO_STREAM_STOP"
    AUDIO_FRAME = "AUDIO_FRAME"
    VOICE_START = "VOICE_START"
    VOICE_DATA = "VOICE_DATA"
    VOICE_STOP = "VOICE_STOP"
    
    # Веб-камера
    WEBCAM_STREAM_START = "WEBCAM_STREAM_START"
    WEBCAM_FRAME = "WEBCAM_FRAME"
    WEBCAM_STREAM_STOP = "WEBCAM_STREAM_STOP"
    
    # Наблюдение
    SCREENSHOT_REQUEST = "SCREENSHOT_REQUEST"
    SCREENSHOT_RESPONSE = "SCREENSHOT_RESPONSE"
    MONITOR_START = "MONITOR_START"
    MONITOR_STOP = "MONITOR_STOP"
    
    # Интерактивная доска
    WHITEBOARD_START = "WHITEBOARD_START"
    WHITEBOARD_STROKE = "WHITEBOARD_STROKE"
    WHITEBOARD_CLEAR = "WHITEBOARD_CLEAR"
    WHITEBOARD_IMAGE = "WHITEBOARD_IMAGE"
    WHITEBOARD_STOP = "WHITEBOARD_STOP"
    
    # Чат
    CHAT_MESSAGE = "CHAT_MESSAGE"
    CHAT_GROUP = "CHAT_GROUP"
    CHAT_HISTORY = "CHAT_HISTORY"
    
    # Файлы
    FILE_SEND_REQUEST = "FILE_SEND_REQUEST"
    FILE_SEND_ACCEPT = "FILE_SEND_ACCEPT"
    FILE_CHUNK = "FILE_CHUNK"
    FILE_COMPLETE = "FILE_COMPLETE"
    FILE_ERROR = "FILE_ERROR"
    
    # Экзамены
    EXAM_START = "EXAM_START"
    EXAM_QUESTION = "EXAM_QUESTION"
    EXAM_ANSWER = "EXAM_ANSWER"
    EXAM_END = "EXAM_END"
    EXAM_RESULT = "EXAM_RESULT"
    
    # Опросы
    POLL_START = "POLL_START"
    POLL_ANSWER = "POLL_ANSWER"
    POLL_RESULT = "POLL_RESULT"
    
    # Викторина
    QUIZ_START = "QUIZ_START"
    QUIZ_BUZZ = "QUIZ_BUZZ"
    QUIZ_ANSWER = "QUIZ_ANSWER"
    QUIZ_RESULT = "QUIZ_RESULT"
    
    # Группы
    GROUP_CREATE = "GROUP_CREATE"
    GROUP_ASSIGN = "GROUP_ASSIGN"
    GROUP_MESSAGE = "GROUP_MESSAGE"
    GROUP_DISBAND = "GROUP_DISBAND"
    
    # Управление
    LOCK_SCREEN = "LOCK_SCREEN"
    UNLOCK_SCREEN = "UNLOCK_SCREEN"
    LOCK_INPUT = "LOCK_INPUT"
    UNLOCK_INPUT = "UNLOCK_INPUT"
    REMOTE_COMMAND = "REMOTE_COMMAND"
    
    # Аудиолаборатория
    AUDIOLAB_START = "AUDIOLAB_START"
    AUDIOLAB_PLAY = "AUDIOLAB_PLAY"
    AUDIOLAB_RECORD = "AUDIOLAB_RECORD"
    AUDIOLAB_SUBMIT = "AUDIOLAB_SUBMIT"
```

---

## 🗄️ База данных

### Схема SQLite:

```sql
-- Студенты
CREATE TABLE students (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    ip_address TEXT,
    mac_address TEXT,
    status TEXT DEFAULT 'offline',
    group_id TEXT,
    last_seen TEXT,
    total_time INTEGER DEFAULT 0,
    metadata TEXT,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

-- Группы
CREATE TABLE groups (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    leader_id TEXT,
    color TEXT DEFAULT '#3B82F6',
    student_ids TEXT,  -- JSON array
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

-- Экзамены
CREATE TABLE exams (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    description TEXT,
    questions TEXT NOT NULL,  -- JSON array
    duration_minutes INTEGER DEFAULT 60,
    pass_score REAL DEFAULT 60.0,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

-- Результаты экзаменов
CREATE TABLE exam_results (
    id TEXT PRIMARY KEY,
    exam_id TEXT NOT NULL,
    student_id TEXT NOT NULL,
    answers TEXT NOT NULL,  -- JSON object
    score REAL DEFAULT 0,
    max_score REAL DEFAULT 0,
    percentage REAL DEFAULT 0,
    started_at TEXT,
    submitted_at TEXT,
    checked BOOLEAN DEFAULT FALSE,
    comments TEXT,
    FOREIGN KEY (exam_id) REFERENCES exams(id),
    FOREIGN KEY (student_id) REFERENCES students(id)
);

-- Аудиокурсы
CREATE TABLE audio_courses (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    audio_file TEXT NOT NULL,
    duration REAL DEFAULT 0,
    segments TEXT,  -- JSON array [{start, end, label}]
    subtitles TEXT,  -- JSON array [{time, text}]
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

-- Записи студентов
CREATE TABLE student_recordings (
    id TEXT PRIMARY KEY,
    course_id TEXT,
    student_id TEXT NOT NULL,
    audio_file TEXT NOT NULL,
    duration REAL DEFAULT 0,
    score REAL,
    comments TEXT,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (course_id) REFERENCES audio_courses(id),
    FOREIGN KEY (student_id) REFERENCES students(id)
);

-- Посещаемость
CREATE TABLE attendance (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id TEXT NOT NULL,
    date TEXT NOT NULL,
    time_in TEXT NOT NULL,
    time_out TEXT,
    duration_seconds INTEGER DEFAULT 0,
    late BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (student_id) REFERENCES students(id)
);

-- История чата
CREATE TABLE chat_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    sender_id TEXT NOT NULL,
    sender_name TEXT NOT NULL,
    recipient_id TEXT,  -- NULL = всем
    group_id TEXT,
    content TEXT NOT NULL,
    message_type TEXT DEFAULT 'text',  -- text, image, file
    timestamp TEXT DEFAULT CURRENT_TIMESTAMP
);

-- Модели класса
CREATE TABLE classroom_layouts (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    layout_data TEXT NOT NULL,  -- JSON {students: [{id, x, y, width, height}]}
    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    updated_at TEXT
);

-- Настройки
CREATE TABLE settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);

-- Черный/белый список сайтов
CREATE TABLE web_lists (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    list_type TEXT NOT NULL,  -- whitelist, blacklist
    url_pattern TEXT NOT NULL,
    description TEXT,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

-- Шаблоны сообщений
CREATE TABLE message_templates (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    category TEXT DEFAULT 'general',
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
);
```

---

## 📦 Сборка и установщик

### Структура сборки:

```
build/
├── Alfarid-Teacher/
│   ├── alfarid_teacher.exe
│   ├── python39.dll
│   ├── PyQt5/
│   ├── cv2/
│   ├── resources/
│   │   ├── icons/
│   │   ├── styles.qss
│   │   └── sounds/
│   └── data/
│       └── classroom.db
│
├── Alfarid-Student/
│   ├── alfarid_student.exe
│   ├── python39.dll
│   ├── PyQt5/
│   └── resources/
│
└── Installer/
    ├── Alfarid-Teacher-Setup.exe
    └── Alfarid-Student-Setup.exe
```

### PyInstaller spec файл (teacher.spec):

```python
# teacher.spec
block_cipher = None

a = Analysis(
    ['teacher_app.py'],
    pathex=['D:\\Projects\\alfarid'],
    binaries=[],
    datas=[
        ('resources', 'resources'),
        ('config.ini', '.'),
    ],
    hiddenimports=[
        'PyQt5.sip',
        'sounddevice',
        'soundfile',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='Alfarid-Teacher',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,  # Без консоли
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='resources/icons/teacher.ico',
)

coll = COLLECT(
    exe,
    a.binaries,
    a.zipfiles,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='Alfarid-Teacher',
)
```

### Скрипт сборки (build.bat):

```batch
@echo off
echo ========================================
echo    ALFARID Build Script
echo ========================================

echo.
echo [1/4] Очистка предыдущей сборки...
rmdir /s /q build 2>nul
rmdir /s /q dist 2>nul

echo.
echo [2/4] Сборка приложения преподавателя...
pyinstaller --clean teacher.spec

echo.
echo [3/4] Сборка приложения студента...
pyinstaller --clean student.spec

echo.
echo [4/4] Создание установщиков...
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer\teacher_setup.iss
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer\student_setup.iss

echo.
echo ========================================
echo    Сборка завершена!
echo ========================================
echo.
echo Установщики:
echo   - dist\Alfarid-Teacher-Setup.exe
echo   - dist\Alfarid-Student-Setup.exe
echo.
pause
```

### Inno Setup скрипт (teacher_setup.iss):

```iss
[Setup]
AppId={{ALFARID-TEACHER-001}
AppName=Alfarid Teacher
AppVersion=1.0.0
AppPublisher=Alfarid Project
AppPublisherURL=https://github.com/alfarid
DefaultDirName={autopf}\Alfarid\Teacher
DefaultGroupName=Alfarid
AllowNoIcons=yes
OutputDir=..\dist
OutputBaseFilename=Alfarid-Teacher-v1.0.0-Setup
SetupIconFile=..\resources\icons\teacher.ico
Compression=lzma2
SolidCompression=yes
WizardStyle=modern
PrivilegesRequired=admin

[Languages]
Name: "russian"; MessagesFile: "compiler:Languages\Russian.isl"
Name: "english"; MessagesFile: "compiler:Default.isl"

[Tasks]
Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"
Name: "autostart"; Description: "Запускать при старте Windows"; GroupDescription: "Автозапуск:"

[Files]
Source: "..\dist\Alfarid-Teacher\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs

[Icons]
Name: "{group}\Alfarid Teacher"; Filename: "{app}\Alfarid-Teacher.exe"
Name: "{group}\Удалить Alfarid Teacher"; Filename: "{uninstallexe}"
Name: "{autodesktop}\Alfarid Teacher"; Filename: "{app}\Alfarid-Teacher.exe"; Tasks: desktopicon

[Registry]
Root: HKCU; Subkey: "Software\Microsoft\Windows\CurrentVersion\Run"; ValueType: string; ValueName: "AlfaridTeacher"; ValueData: """{app}\Alfarid-Teacher.exe"""; Flags: uninsdeletevalue; Tasks: autostart

[Run]
Filename: "{app}\Alfarid-Teacher.exe"; Description: "{cm:LaunchProgram,Alfarid Teacher}"; Flags: nowait postinstall skipifsilent

[Code]
function InitializeSetup(): Boolean;
begin
  Result := True;
  // Проверка .NET Framework, VC++ Redistributable и т.д.
end;
```

---

## 📅 План разработки

### Версия 1.0 (MVP) — Критические функции

**Срок: 4-6 недель**

| Неделя | Задачи |
|--------|--------|
| 1 | Исправить TCP буферизацию, добавить голосовую связь |
| 2 | Реализовать наблюдение за студентами, скриншоты |
| 3 | Интерактивная доска (базовая), запись урока |
| 4 | Веб-камера, экспорт HTML отчетов |
| 5 | Плавающая панель студента, готовые сообщения |
| 6 | Тестирование, сборка, установщик |

### Версия 1.1 — Расширенные функции

**Срок: 3-4 недели после v1.0**

- Викторина с таймером
- Удаленное управление ПК студента
- Чат по группам/темам с историей
- Редактор аудиокурсов
- Импорт студентов из Excel
- Блокировка USB/CD

### Версия 1.2 — Полный функционал

**Срок: 2-3 недели после v1.1**

- Система награждений
- Capture box/dongle
- Внешние источники аудио
- Блокировка принтера
- Уровень заряда батареи
- Мобильное приложение (планирование)

---

## ✅ Чек-лист готовности v1.0

### Функционал:
- [ ] Трансляция экрана с голосом
- [ ] Веб-камера
- [ ] Интерактивная доска
- [ ] Наблюдение за студентами
- [ ] Запись урока
- [ ] Экспорт отчетов HTML
- [ ] Плавающая панель студента
- [ ] Голосовой чат
- [ ] Блокировка клавиатуры/мыши

### Инфраструктура:
- [ ] Иконки приложения
- [ ] QSS темы (светлая/темная)
- [ ] Локализация (RU/EN)
- [ ] PyInstaller сборка
- [ ] Inno Setup установщик
- [ ] README с инструкциями
- [ ] Документация пользователя

### Тестирование:
- [ ] Работа в локальной сети
- [ ] Работа через WiFi
- [ ] 10+ студентов одновременно
- [ ] 30+ минут без сбоев
- [ ] Windows 7/10/11

---

## 📚 Ссылки и ресурсы

- [Линко V8.3 Руководство](docs/rukovodstvo-polzovatelya-uchenika-po-linko-v8.3.pdf)
- [PyQt5 Documentation](https://doc.qt.io/qtforpython/)
- [OpenCV Python](https://docs.opencv.org/master/d6/d00/tutorial_py_root.html)
- [sounddevice](https://python-sounddevice.readthedocs.io/)
- [PyInstaller](https://pyinstaller.org/en/stable/)
- [Inno Setup](https://jrsoftware.org/isinfo.php)

---

**Дата создания:** 02.01.2026  
**Версия документа:** 1.0  
**Автор:** Alfarid Development Team

---

*© 2026 Alfarid Project — Образование без границ*

