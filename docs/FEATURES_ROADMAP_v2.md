# Roadmap развития Alfarid v2.0

## Фаза 4: Корпоративные функции (2-3 недели)

### 4.1 Админ-панель (Web)
**Технологии**: FastAPI + React или Django Admin

**Функции:**
- ✅ Управление учителями (добавить/удалить/редактировать)
- ✅ Управление студентами
- ✅ Создание классов и групп
- ✅ Расписание занятий
- ✅ Статистика использования
- ✅ Логи активности
- ✅ Настройки системы

**Структура:**
```
alfarid-admin/
├── backend/
│   ├── main.py              # FastAPI сервер
│   ├── models.py            # База данных (PostgreSQL/SQLite)
│   ├── auth.py              # Аутентификация
│   └── api/
│       ├── teachers.py
│       ├── students.py
│       ├── classes.py
│       └── analytics.py
└── frontend/
    ├── src/
    │   ├── components/
    │   ├── pages/
    │   └── api/
    └── package.json
```

### 4.2 Видеозапись урока

**Что записывать:**
1. Экран преподавателя
2. Голос преподавателя
3. Веб-камера преподавателя
4. Интерактивная доска
5. Чат сообщения
6. События (студент поднял руку, и т.д.)

**Формат записи:**
```
lesson_2026-01-03_15-30.alfrec/
├── metadata.json          # Информация о уроке
├── screen/
│   ├── 00001.jpg         # Кадры экрана (JPEG)
│   ├── 00002.jpg
│   └── ...
├── audio.mp3             # Аудио дорожка
├── webcam.mp4            # Веб-камера (опционально)
├── whiteboard/
│   └── commands.json     # Команды рисования
├── chat.json             # Чат
└── events.json           # События урока
```

**Модуль записи:**
```python
# src/recording/lesson_recorder.py

class LessonRecorder:
    """Запись урока"""
    
    def __init__(self, output_dir="recordings"):
        self.recording = False
        self.start_time = None
        
        # Рекордеры
        self.screen_recorder = ScreenRecorder()
        self.audio_recorder = AudioRecorder()
        self.event_logger = EventLogger()
    
    def start_recording(self, lesson_name: str):
        """Начать запись урока"""
        timestamp = datetime.now().strftime("%Y-%m-%d_%H-%M")
        self.output_path = Path(output_dir) / f"{lesson_name}_{timestamp}.alfrec"
        self.output_path.mkdir(parents=True, exist_ok=True)
        
        # Метаданные
        self.metadata = {
            'lesson_name': lesson_name,
            'start_time': timestamp,
            'teacher': self.teacher_name,
            'students': [s.name for s in self.students]
        }
        
        # Запускаем рекордеры
        self.screen_recorder.start(self.output_path / "screen")
        self.audio_recorder.start(self.output_path / "audio.mp3")
        
        self.recording = True
        logger.info(f"Запись урока начата: {self.output_path}")
    
    def stop_recording(self):
        """Остановить запись"""
        if not self.recording:
            return
        
        self.screen_recorder.stop()
        self.audio_recorder.stop()
        
        # Сохраняем метаданные
        self.metadata['end_time'] = datetime.now().strftime("%Y-%m-%d_%H-%M")
        self.metadata['duration'] = time.time() - self.start_time
        
        with open(self.output_path / "metadata.json", 'w') as f:
            json.dump(self.metadata, f, indent=2)
        
        self.recording = False
        logger.info("Запись урока завершена")
    
    def add_event(self, event_type: str, data: dict):
        """Добавить событие в запись"""
        if self.recording:
            self.event_logger.add({
                'timestamp': time.time() - self.start_time,
                'type': event_type,
                'data': data
            })
```

**Плеер для просмотра:**
```python
# src/recording/lesson_player.py

class LessonPlayer:
    """Воспроизведение записи урока"""
    
    def __init__(self, recording_path: str):
        self.recording = self._load_recording(recording_path)
        self.current_time = 0
    
    def play(self):
        """Воспроизведение"""
        # Синхронизация экрана + аудио + события
        pass
    
    def seek(self, timestamp: float):
        """Перемотка на время"""
        pass
    
    def export_to_mp4(self, output_path: str):
        """Экспорт в MP4 (FFmpeg)"""
        # screen/*.jpg + audio.mp3 -> video.mp4
        import subprocess
        subprocess.run([
            'ffmpeg',
            '-framerate', '24',
            '-i', 'screen/%05d.jpg',
            '-i', 'audio.mp3',
            '-c:v', 'libx264',
            '-c:a', 'aac',
            output_path
        ])
```

### 4.3 Оптимизация производительности

**Multicast для трансляции:**
- UDP multicast вместо 30 TCP соединений
- Меньше нагрузки на CPU
- Меньше трафика

**Адаптивное качество:**
- Автоматическое снижение качества при большом количестве студентов
- Профили: small (1-10), medium (11-20), large (21-50)

**Сжатие:**
- JPEG качество зависит от количества студентов
- Использование H.264 для видео (через FFmpeg)
- Opus для аудио

### 4.4 Профессиональный установщик

**Windows:**
```
Alfarid-Setup-1.0.0.exe
├── Teacher/
│   ├── Alfarid_Teacher.exe
│   ├── ffmpeg.dll
│   └── dependencies/
├── Student/
│   ├── Alfarid_Student.exe
│   └── dependencies/
└── Admin/
    └── admin-panel/
```

**Инструменты:**
- **PyInstaller** — сборка EXE из Python
- **Inno Setup** — профессиональный установщик
- **Auto-update** — автоматические обновления

## Фаза 5: Расширенные функции (3-4 недели)

### 5.1 Облачная синхронизация
- Сохранение уроков в облако
- Доступ из любого места
- Общие файлы

### 5.2 Мобильное приложение (Android/iOS)
- Студенты могут подключаться с телефона
- Push-уведомления
- Базовый просмотр

### 5.3 Интеграция с LMS
- Moodle, Google Classroom
- Экспорт оценок
- Импорт студентов

## Оценка времени:

| Фаза | Задача | Время |
|------|--------|-------|
| 4.1 | Админ-панель (Web) | 1 неделя |
| 4.2 | Видеозапись | 1 неделя |
| 4.3 | Оптимизация | 3-4 дня |
| 4.4 | Установщик | 2-3 дня |
| **Итого** | **Фаза 4** | **2-3 недели** |

## Альтернатива: Переписать на C#

### Если решите переписать на C#:

**Преимущества:**
- Лучшая производительность
- Нативные Windows API
- WPF для красивого UI
- Легче создать MSI установщик
- Entity Framework для БД
- SignalR для real-time

**Структура C# версии:**
```
Alfarid.sln
├── Alfarid.Core/          # Общая логика
├── Alfarid.Teacher/       # WPF приложение учителя
├── Alfarid.Student/       # WPF приложение студента
├── Alfarid.Server/        # ASP.NET Core (админка)
├── Alfarid.Database/      # Entity Framework
└── Alfarid.Installer/     # WiX установщик
```

**Время на переписывание:** 2-3 месяца

## Моя рекомендация:

### 🎯 **Оптимальный план:**

1. **Сейчас (1-2 недели):**
   - Доработать Python версию
   - Добавить видеозапись
   - Оптимизировать для 30+ студентов
   - Создать базовую админку (Django Admin)
   - Собрать EXE с PyInstaller

2. **После первых пользователей (1-2 месяца):**
   - Собрать feedback
   - Понять реальные требования
   - Найти узкие места

3. **Если нужна enterprise версия:**
   - Переписать на C# (.NET 8)
   - Создать профессиональную архитектуру
   - Добавить облако и мобильные приложения

### Почему такой план?

✅ **Не теряем время** — Python версия уже работает  
✅ **Быстрый вывод на рынок** — можно тестировать с реальными пользователями  
✅ **Feedback-driven** — переписываем только если действительно нужно  
✅ **Минимальный риск** — можно продавать текущую версию параллельно разработке новой  

## Что делать прямо сейчас?

Выберите один из путей:

### **Путь A: Довести Python до продакшена** ⭐ (рекомендую)
```bash
# Следующие задачи (по приоритету):
1. Оптимизация для 30+ студентов (multicast)
2. Видеозапись уроков
3. Базовая админка (Django Admin)
4. EXE установщик (PyInstaller + Inno Setup)
5. Тестирование с реальными пользователями
```

### **Путь B: Переписать на C#**
```bash
# Начать заново с архитектуры
1. Спроектировать схему БД
2. Создать backend на ASP.NET Core
3. Создать Teacher app на WPF
4. Создать Student app на WPF
5. Реализовать всё заново (2-3 месяца)
```

---

**Мой совет как senior dev:** Не переписывайте сейчас. Доведите Python версию до MVP, соберите пользователей, заработайте первые деньги, а потом решите нужна ли C# версия.

C# имеет смысл только если:
- Клиенты требуют Windows-only решение
- Нужна интеграция с Active Directory
- Критична производительность при 50+ студентов
- Есть бюджет на 2-3 месяца разработки

Что скажете? По какому пути пойдём?



